# Universal Static Website Deployment System
# Template for new projects - copy and modify this file

# PROJECT CONFIGURATION
PROJECT_NAME=your-project-name
DOMAIN_URL=https://your-domain.com
BUILD_DIRECTORY=build
ADMIN_DIRECTORY=admin

# FTP CONFIGURATION (Primary deployment method)
FTP_HOST=ftp.your-domain.com
FTP_USER=your-ftp-username
FTP_PASSWORD=your-ftp-password
FTP_REMOTE_PATH=/httpdocs

# SSH CONFIGURATION (Backup deployment method)
SSH_HOST=your-ssh-host.com
SSH_USER=your-ssh-username
SSH_PASSWORD=your-ssh-password
SSH_REMOTE_PATH=your-domain.com

# DEPLOYMENT SETTINGS
UPLOAD_ADMIN=true
UPLOAD_FRONTEND=true
FTP_UPLOAD_BACKEND=false

# ADMIN CREDENTIALS (Change after first deployment!)
ADMIN_DEFAULT_USER=admin
ADMIN_DEFAULT_PASS=change-me-123

# DEPLOYMENT PATHS
LOCAL_FRONTEND_PATH=build
LOCAL_ADMIN_PATH=admin
REMOTE_FRONTEND_PATH=/httpdocs
REMOTE_ADMIN_PATH=/httpdocs/admin

# OPTIONAL: Database configuration for admin setup
DB_HOST=your-db-host
DB_PORT=3306
DB_NAME=your-database-name
DB_USER=your-db-username
DB_PASS=your-db-password
