# === Core App Config ===
NODE_ENV=development
PORT=3000
SESSION_SECRET=change_me_session_secret

# === Database ===
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name
# Optional socket path (overrides host/port when set)
# DB_SOCKET_PATH=/var/run/mysqld/mysqld.sock
DB_SLOW_THRESHOLD_MS=250

# === Multi-Tenant / Site ===
SITE_URL=https://example.com

# === Admin Access Token (optional quick admin override) ===
# ADMIN_ACCESS_TOKEN=some_long_random_string

# === AI Config (fallback if DB row missing) ===
# AI_MAX_DAILY_CALLS=500

# === Deployment (DO NOT COMMIT REAL SECRETS) ===
# These are placeholders; in CI prefer GitHub Secrets instead of committing .env.
# FTP Deployment (optional)
FTP_HOST=ftp.example.com
FTP_USER=ftp_user
FTP_PASSWORD=ftp_password
FTP_REMOTE_DIR=/path/on/server

# SSH Deployment (preferred)
DEPLOY_SSH_HOST=hosting223936.ae94b.netcup.net
DEPLOY_SSH_USER=hosting223936
# Path where releases / app live
DEPLOY_WEB_ROOT=/home/hosting223936/panda
# If using pm2 process manager on server
PM2_APP_NAME=panda

# If you want the workflow to run npm install --production via SSH set this to true
DEPLOY_POST_INSTALL=true

# === Feature Flags (potential future overrides) ===
# FEATURE_FLAGS_PRELOAD=true

# === Security Headers (future tuning) ===
# FORCE_HTTPS=true
# HSTS_MAX_AGE=31536000

# Copy this file to .env and fill in real values (NEVER commit real .env with secrets)
