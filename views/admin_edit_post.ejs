<%- include('partials/header') %>
<% /* Slim legacy stub: full blog editor relocated to /editors */ %>
<%- include('partials/admin_nav') %>
<div class="container py-5">
    <div class="alert alert-info shadow-sm">
        <h4 class="alert-heading mb-2">Blog Editor umgezogen</h4>
        <p class="mb-2">Die Bearbeitung von Blog-Beiträgen findet jetzt ausschließlich im <strong>Editors Center</strong> statt.</p>
        <ul class="small mb-3">
            <li>Neuer Beitrag: <code>/editors/posts/new</code></li>
            <li>Vorhandenen Beitrag: <code>/editors/posts/edit/&lt;id&gt;</code></li>
            <li>Medien: <code>/editors/media</code></li>
        </ul>
        <% if (post) { %>
            <a href="/editors/posts/edit/<%= post.id %>" class="btn btn-primary me-2">Beitrag öffnen</a>
        <% } else { %>
            <a href="/editors/posts/new" class="btn btn-primary me-2">Neuen Beitrag erstellen</a>
        <% } %>
        <a href="/editors/posts" class="btn btn-outline-secondary">Zur Liste</a>
    </div>
    <p class="text-muted small mb-1">Diese Seite bleibt temporär für alte interne Links bestehen und wird bald entfernt.</p>
    <p class="text-muted small">Archivierte Original-Datei: <code>views/legacy/admin_edit_post.ejs</code> (falls vorhanden).</p>
</div>
<script nonce="<%= cspNonce %>">
// Soft redirect to new editors route; keep DOM simple to avoid EJS lint noise
setTimeout(()=>{
    var target = '<%- (post ? ('/editors/posts/edit/'+post.id) : '/editors/posts/new') %>';
    if(document.visibilityState==='visible') window.location.replace(target);
}, 1200);
</script>
<%- include('partials/footer') %>
